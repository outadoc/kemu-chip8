include(":lib-chip8")
include(":lib-kemu")
include(":app-chip8-desktop")
include(":app-chip8-browser")

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        jcenter()
        mavenCentral()
    }
}