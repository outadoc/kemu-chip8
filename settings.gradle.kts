include(":lib-chip8")
include(":lib-kemu")
include(":app-chip8-desktop")

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        jcenter()
        mavenCentral()
    }
}